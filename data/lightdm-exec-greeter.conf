[greeter]
#username the greeter should use to authenticate
username=user

#Checks the exitcode of the given script. If the exitcode is the same as the requiredExitcode the greeter will allow login
exec=

#defines on which exitcode the greeter should start the login
requiredExitcode=0

#how many ms to wait before the next exec try starts.
execRetryTimeout=1000
